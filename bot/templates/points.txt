🎯 Deine Rebellion-Punkte

Hallo {{ first_name }}!

━━━━━━━━━━━━━━━━━━━━━━
💫 GESAMTPUNKTE
━━━━━━━━━━━━━━━━━━━━━━

{{ total_points }} Punkte
{% if ranking and total_users %}
📊 Rang: {{ ranking }} von {{ total_users }}
{% endif %}

━━━━━━━━━━━━━━━━━━━━━━
📊 BREAKDOWN
━━━━━━━━━━━━━━━━━━━━━━

🎉 Party-Content: {{ party_photos_count }} × 1 = {{ party_points }} Punkte
{% if film_submitted > 0 %}
🎬 Film-Referenzen: {{ film_approved }}/{{ film_submitted }} korrekt × 20 = {{ film_points }} Punkte
{% else %}
🎬 Film-Referenzen: 0 × 20 = 0 Punkte
{% endif %}
👥 Team-Beitritt: {{ team_points }} Punkte
🧩 Puzzle gelöst: {{ puzzle_points }} Punkte

{% if team_name %}
━━━━━━━━━━━━━━━━━━━━━━
👥 DEIN TEAM
━━━━━━━━━━━━━━━━━━━━━━

Team: {{ team_name }}
{% endif %}

{% if recognized_films %}
━━━━━━━━━━━━━━━━━━━━━━
✅ ERKANNTE FILME
━━━━━━━━━━━━━━━━━━━━━━

{% for film in recognized_films %}• {{ film }}
{% endfor %}
{% endif %}

{% if top_players %}
━━━━━━━━━━━━━━━━━━━━━━
🏆 TOP SPIELER
━━━━━━━━━━━━━━━━━━━━━━

{% for player in top_players %}{{ loop.index }}. {{ player.name }}: {{ player.points }} Punkte{% if player.team %} ({{ player.team }}){% endif %}
{% endfor %}
{% endif %}

{% if top_teams %}
━━━━━━━━━━━━━━━━━━━━━━
👥 TOP TEAMS
━━━━━━━━━━━━━━━━━━━━━━

{% for team in top_teams %}{{ loop.index }}. {{ team.team_name }}: {{ team.total_points }} Punkte ({{ team.member_count }} Mitglieder)
{% endfor %}
{% endif %}

━━━━━━━━━━━━━━━━━━━━━━

Die Rebellion zählt auf dich! 💪
